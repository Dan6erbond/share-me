import Layout from "@/components/docs/layout";

# Installation

Share Me uses PocketBase and Next.js as its core stack. This means it can run as a standalone app, on Vercel, or using Docker.

## Quick Start

The recommended way to install Share Me is using Docker. See the [Docker](/installation/docker) guides.

export default ({ children }) => <Layout>{children}</Layout>;
