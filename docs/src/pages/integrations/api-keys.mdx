import Layout from "@/components/docs/layout";
import Image from "next/image";

# API Keys

Share Me allows users to create custom integrations via its API using API keys. Users can create and manage API keys under the settings page.

![API Key Management](/settings.webp)

export default ({ children }) => <Layout>{children}</Layout>;
